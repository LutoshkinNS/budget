services:
  app:
    build:
      context: ..
      dockerfile: frontend/Dockerfile
    container_name: budget_frontend
    restart: always
    ports:
      - '3001:8080'
    environment:
      VITE_API_URL: ${VITE_API_URL}
      VITE_NGROK_DOMAIN: ${VITE_NGROK_DOMAIN}
